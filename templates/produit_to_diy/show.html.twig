{% extends 'base.html.twig' %}

{% block title %}ProduitToDiy{% endblock %}

{% block body %}
    <h1>ProduitToDiy</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produit_to_diy.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_produit_to_diy_index') }}">back to list</a>

    <a href="{{ path('app_produit_to_diy_edit', {'id': produit_to_diy.id}) }}">edit</a>

    {{ include('produit_to_diy/_delete_form.html.twig') }}
{% endblock %}
