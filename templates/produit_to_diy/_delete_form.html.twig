<form method="post" action="{{ path('app_produit_to_diy_delete', {'id': produit_to_diy.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ produit_to_diy.id) }}">
    <button class="btn">Delete</button>
</form>
