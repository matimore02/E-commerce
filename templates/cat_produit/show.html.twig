{% extends 'base.html.twig' %}

{% block title %}CatProduit{% endblock %}

{% block body %}
    <h1>CatProduit</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cat_produit.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ cat_produit.libelle }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cat_produit_index') }}">back to list</a>

    <a href="{{ path('app_cat_produit_edit', {'id': cat_produit.id}) }}">edit</a>

    {{ include('cat_produit/_delete_form.html.twig') }}
{% endblock %}
