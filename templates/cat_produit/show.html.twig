{% block body %}
<link rel="stylesheet" href="{{ asset('asset/css/styleGstCategorie.css') }}">

   <nav class="sidebar">
		<h6><a href="#">RETOUR VERS ECO-SERVICE</a></h6>
		<h4>Sommaire Gestion d'Application</h4>
		
		<ul class="nav-links">
			<li><a href="{{ path('app_adresse_index') }}" >Adresse</a></li>
            <li><a href="{{ path('app_cat_produit_index') }}" >Catégorie Produit</a></li>
            <li><a href="{{ path('app_commenter_index') }}">Commentaire</a></li>
            <li><a href="{{ path('app_produit_index') }}">Produit</a></li>
            <li><a href="{{ path('app_diy_index') }}">DIY</a></li>

		</ul>
	</nav>

 <section class="admin-section">
		<h2>Gestion des Comptes</h2>
		<table class="account-table">
			<thead>
				<tr>
					<th>id</th>
                    <th>libellé</th>
                    <th>retour</th>
                    <th>modifier</th>


				</tr>
			</thead>
			<tbody>
            <tr>
                <td>{{ cat_produit.id }}</td>
                <td>{{ cat_produit.libelle }}</td>
                <td><a class="ban-button" href="{{ path('app_cat_produit_index') }}">retour</a></td>
                <td><a class="ban-button" href="{{ path('app_cat_produit_edit', {'id': cat_produit.id}) }}">modifier</a></td>

            </tr>
			</tbody>
		</table>
	</section>


    {{ include('cat_produit/_delete_form.html.twig') }}
{% endblock %}
