{% block body %}

<link rel="stylesheet" href="{{ asset('asset/css/styleGstCategorie.css') }}">

    <nav class="sidebar">
		<h6><a href="#">RETOUR VERS ECO-SERVICE</a></h6>
		<h4>Sommaire Gestion d'Application</h4>
		
		<ul class="nav-links">
			<li><a href="#">Comptes</a></li>
			<li><a href="#">Commandes</a></li>
			<li><a href="#">Catégories</a></li>
			<li><a href="#">Produits</a></li>
			<li><a href="#">Stockage</a></li>
			<li><a href="#">Historique E/S</a></li>
	
		</ul>
	</nav>

   <section class="admin-section">
		<h2>Gestion des Comptes</h2>
		<table class="account-table">
			<thead>
				<tr>
					<th>Nom</th> <!-- renvoie vers la page commande pour voir les commandes de ce nom -->
                    <th>Modifier</th>
                    <th>Supprimer</th>
                    <th>Voir </th>
				</tr>
			</thead>
			<tbody>
            {% for cat_produit in cat_produits %}
            <tr>
                <td>{{ cat_produit.libelle }}</td>
                <td><a class="ban-button" href="{{ path('app_cat_produit_edit', {'id': cat_produit.id}) }}">edit</a></td>
                <td> {{ include('cat_produit/_delete_form.html.twig') }}</td>
                <td><a class="ban-button" href="{{ path('app_cat_produit_show', {'id': cat_produit.id}) }}">show</a></td>>
            </tr>
        {% endfor %}
			</tbody>
		</table>
	</section>

    <a href="{{ path('app_cat_produit_new') }}">Create new</a>
{% endblock %}
